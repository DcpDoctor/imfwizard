{
  "identifier": "default",
  "description": "Default capabilities for IMF Wizard",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute",
    "dialog:allow-open"
  ]
}
